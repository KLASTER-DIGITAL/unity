"use client";

import React from 'react';
import { SimpleTest } from '../../SimpleTest';

interface SettingsTabProps {
  className?: string;
}

export const SettingsTab: React.FC<SettingsTabProps> = ({ className }) => {
  return <SimpleTest />;
};