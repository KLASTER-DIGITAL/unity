name: Query Performance Monitor

# Еженедельный мониторинг производительности запросов
# Запускается каждую пятницу в 10:00 UTC (13:00 MSK)
on:
  schedule:
    - cron: '0 10 * * 5' # Каждую пятницу в 10:00 UTC
  workflow_dispatch: # Ручной запуск

jobs:
  monitor:
    name: Monitor Query Performance
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Query Performance Monitor
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          npx ts-node scripts/monitor-query-performance.ts

      - name: Upload report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: query-performance-report
          path: docs/reports/query-performance-*.md
          retention-days: 90

      - name: Create Issue on Alert
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const reportFiles = fs.readdirSync('docs/reports')
              .filter(f => f.startsWith('query-performance-'))
              .sort()
              .reverse();
            
            if (reportFiles.length === 0) {
              console.log('No report files found');
              return;
            }
            
            const latestReport = fs.readFileSync(
              `docs/reports/${reportFiles[0]}`,
              'utf8'
            );
            
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '⚠️ Query Performance Alert',
              body: `## Query Performance Monitor обнаружил проблемы

${latestReport}

---

**Действия:**
1. Проверить отчет выше
2. Оптимизировать медленные запросы
3. Проверить Cache Hit Rate
4. Закрыть issue после исправления

**Автоматически создано:** ${new Date().toISOString()}`,
              labels: ['performance', 'database', 'automated']
            });

      - name: Commit report
        if: always()
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add docs/reports/query-performance-*.md
          git diff --staged --quiet || git commit -m "📊 Query Performance Report - $(date +%Y-%m-%d)"
          git push || echo "Nothing to commit"

